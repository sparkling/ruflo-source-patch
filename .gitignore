.DS_Store
*.rsp-backup
*.tgz
node_modules/

# This repository's local Codex files (other projects may intentionally track .codex/config.toml)
.codex/

# Claude Flow runtime data
.claude-flow/data/
.claude-flow/logs/

# Environment variables
.env
.env.local
.env.*.local

# ruflo-add-codex:gitignore — secrets + runtime
.claude-flow/sessions/
*.bak


# runtime memory + vector state (regenerated by `ruflo memory init`; never source — matches sibling repos)
.swarm/
.claude-flow/
ruvector.db
agentdb.rvf

.claude/.bundle-backup/

# .claude runtime state (memory DB + proven-config harness output; regenerated, not source)
.claude/memory.db
.claude/memory.db-shm
.claude/memory.db-wal
.claude/proven-config.json
.claude/.proven-config-version
