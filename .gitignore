.DS_Store
*.rsp-backup
*.tgz
node_modules/

# Codex local configuration
.codex/

# Claude Flow runtime data
.claude-flow/data/
.claude-flow/logs/

# Environment variables
.env
.env.local
.env.*.local

# ruflo-add-codex:gitignore — secrets + runtime
.claude-flow/sessions/
*.bak


# runtime memory + vector state (regenerated by `ruflo memory init`; never source — matches sibling repos)
.swarm/
.claude-flow/
ruvector.db
agentdb.rvf

# .codex = local overrides (add-codex.sh convention: .agents/config.toml is the committed project config)
.codex/
