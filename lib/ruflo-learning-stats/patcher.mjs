// #2245 / ADR-075 follow-up: correct reporting without moving or training data.
// Reversible exact anchors compose with the existing cwd patch. Unknown upstream
// source fails closed. Retire only when native fixture behaviour passes this suite.
export const UNIFIED_OLD = "export async function getUnifiedLearningStats() {\n    const intel = getIntelligenceStats();\n    const sonaCoord = sonaCoordinator;\n    const bank = reasoningBank;\n    // SONA in-memory view\n    const sonaAvailable = !!sonaCoord;\n    let sonaStats = { trajectoriesTotal: 0, patternsLearned: 0, reasoningBankSize: 0, avgAdaptationTimeMs: 0 };\n    if (sonaCoord) {\n        try {\n            const s = sonaCoord.stats?.() ?? {};\n            sonaStats = {\n                trajectoriesTotal: Number(s.trajectoriesTotal ?? s.trajectoriesProcessed ?? 0),\n                patternsLearned: Number(s.totalPatterns ?? s.patternsLearned ?? 0),\n                reasoningBankSize: bank?.stats?.()?.patternCount ?? 0,\n                avgAdaptationTimeMs: sonaCoord.getAvgAdaptationTime?.() ?? 0,\n            };\n        }\n        catch { /* SONA not yet initialised */ }\n    }\n    // memory-bridge\n    let bridgeStats = {\n        totalEntries: 0, perNamespace: {}, source: 'memory-bridge (skipped)', reachable: false,\n    };\n    try {\n        const mb = await import('./memory-bridge.js');\n        bridgeStats = await mb.getMemoryBridgeStats();\n    }\n    catch { /* bridge module not loadable */ }\n    // neural store\n    let neuralStats = {\n        patternCount: 0, byType: {}, modelCount: 0, source: 'neural store (skipped)',\n    };\n    try {\n        const nt = await import('../mcp-tools/neural-tools.js');\n        neuralStats = nt.getNeuralStoreStats();\n    }\n    catch { /* neural module not loadable */ }\n    // Consistency notes \u2014 describe (don't enforce) the cross-store relationships\n    const sonaTracksGlobalDelta = sonaStats.trajectoriesTotal - intel.trajectoriesRecorded;\n    const notes = [];\n    if (sonaAvailable && Math.abs(sonaTracksGlobalDelta) > 2) {\n        notes.push(`sona.trajectoriesTotal (${sonaStats.trajectoriesTotal}) drifts from globalStats.trajectoriesRecorded (${intel.trajectoriesRecorded}) by ${sonaTracksGlobalDelta} \u2014 expected to track within \u00b11`);\n    }\n    if (intel.patternsLearned > 0 && neuralStats.patternCount === 0) {\n        notes.push(`globalStats reports ${intel.patternsLearned} patterns learned but neural_patterns store is empty \u2014 pretrain has not written here, or trajectory-end isn't promoting patterns to the neural store yet`);\n    }\n    if (!bridgeStats.reachable) {\n        notes.push('memory-bridge unreachable \u2014 bridge-dependent counters (post-edit/-command persistence, pretrain bundle) will show 0');\n    }\n    return {\n        global: {\n            patternsLearned: intel.patternsLearned,\n            trajectoriesRecorded: intel.trajectoriesRecorded,\n            signalsProcessed: intel.signalsProcessed,\n            lastAdaptation: intel.lastAdaptation,\n            source: '.claude-flow/neural/stats.json (globalStats)',\n        },\n        sona: {\n            ...sonaStats,\n            source: 'sonaCoordinator (in-memory, resets per process)',\n            available: sonaAvailable,\n        },\n        memoryBridge: bridgeStats,\n        neuralPatterns: neuralStats,\n        consistency: {\n            sonaTracksGlobal: sonaAvailable ? Math.abs(sonaTracksGlobalDelta) <= 1 : true,\n            sonaTracksGlobalDelta,\n            notes,\n        },\n        generatedAt: new Date().toISOString(),\n    };\n}";
export const UNIFIED_NEW = "export async function getUnifiedLearningStats() {\n    // ruflo-source-patch (ruvnet/ruflo#2245): report each store in its own scope.\n    const intel = getIntelligenceStats();\n    const count = (value) => typeof value === 'number' && Number.isFinite(value) && value >= 0 ? value : null;\n    let s = null;\n    let bankStats = null;\n    let sonaError;\n    let bankError;\n    try { s = sonaCoordinator?.stats?.() ?? null; }\n    catch (error) { sonaError = String(error?.message || error); }\n    try { bankStats = reasoningBank?.stats?.() ?? null; }\n    catch (error) { bankError = String(error?.message || error); }\n    const read = (file, validate) => {\n        try {\n            if (!existsSync(file)) return { available: true, readStatus: 'absent', value: null };\n            const value = JSON.parse(readFileSync(file, 'utf-8'));\n            if (!validate(value)) throw new Error('Invalid learning store shape');\n            return { available: true, readStatus: 'ok', value };\n        } catch (error) { return { available: false, readStatus: 'error', value: null, error: String(error?.message || error) }; }\n    };\n    const diskGlobal = read(getStatsPath(), v => v && !Array.isArray(v)\n        && ['patternsLearned', 'trajectoriesRecorded', 'signalsProcessed'].every(k => count(v[k]) !== null));\n    const diskBank = read(getPatternsPath(), v => Array.isArray(v)\n        && v.every(p => p && typeof p === 'object' && typeof p.id === 'string'));\n    const retained = diskBank.available ? (diskBank.value?.length ?? 0) : null;\n    const persistedGlobal = diskGlobal.value;\n    const buffered = count(s?.trajectoryCount ?? s?.trajectoriesTotal ?? s?.trajectoriesProcessed);\n    let bridgeStats = { totalEntries: null, perNamespace: {}, source: 'memory-bridge unavailable', reachable: false };\n    try {\n        const mb = await import('./memory-bridge.js');\n        bridgeStats = await mb.getMemoryBridgeStats();\n    } catch (error) { bridgeStats.error = String(error?.message || error); }\n    let neuralStats = { patternCount: null, byType: {}, modelCount: null, source: 'neural store unavailable', available: false };\n    try {\n        const nt = await import('../mcp-tools/neural-tools.js');\n        neuralStats = nt.getNeuralStoreStats();\n    } catch (error) { neuralStats.error = String(error?.message || error); }\n    const notes = [\n        'SONA trajectories are a bounded process-local buffer; lifetime global counters are not an equality invariant.',\n        'ReasoningBank patterns and neural models/patterns are separate retained stores. An empty neural model store does not imply failed trajectory retention.'\n    ];\n    if (!s || buffered === null) notes.push('SONA buffer statistics unavailable; null is not zero activity.');\n    if (retained === null) notes.push('ReasoningBank statistics unavailable; inspect its consumer before concluding that patterns are absent.');\n    if (!diskGlobal.available) notes.push('Persisted learning counters unreadable; see global.error.');\n    if (!bridgeStats.reachable) notes.push('memory-bridge unreachable; entry counts are unavailable.');\n    if (neuralStats.available === false) notes.push('neural model-store statistics unavailable; see its readStatus/error.');\n    return {\n        global: {\n            patternsLearned: diskGlobal.available ? (persistedGlobal?.patternsLearned ?? 0) : null,\n            trajectoriesRecorded: diskGlobal.available ? (persistedGlobal?.trajectoriesRecorded ?? 0) : null,\n            signalsProcessed: diskGlobal.available ? (persistedGlobal?.signalsProcessed ?? 0) : null,\n            lastAdaptation: persistedGlobal?.lastAdaptation ?? null,\n            source: getStatsPath() + ' (persisted globalStats)',\n            scope: 'persisted learning-event counters; not distinct retained artefacts or measured improvement',\n            available: diskGlobal.available,\n            readStatus: diskGlobal.readStatus,\n            ...(diskGlobal.error ? { error: diskGlobal.error } : {}),\n        },\n        globalInProcess: {\n            patternsLearned: intel.patternsLearned,\n            trajectoriesRecorded: intel.trajectoriesRecorded,\n            signalsProcessed: intel.signalsProcessed,\n            initialized: intelligenceInitialized,\n            scope: 'process snapshot; may lag other writers or contain unflushed events',\n        },\n        sona: {\n            trajectoriesTotal: buffered,\n            trajectoriesBuffered: buffered,\n            patternsLearned: retained,\n            patternsLearnedMeaning: 'compatibility alias for reasoningBank.patternCount, not a cumulative training counter',\n            reasoningBankSize: retained,\n            avgAdaptationTimeMs: count(s?.avgAdaptationMs),\n            source: 'sonaCoordinator (bounded in-memory buffer, resets per process)',\n            scope: 'current-process buffer only',\n            available: !!s,\n            ...(sonaError ? { error: sonaError } : {}),\n        },\n        reasoningBank: {\n            patternCount: retained,\n            source: getPatternsPath() + ' (LocalReasoningBank)',\n            scope: 'distinct patterns persisted across sessions; read from disk',\n            inMemoryCount: count(bankStats?.patternCount),\n            available: diskBank.available,\n            readStatus: diskBank.readStatus,\n            ...(diskBank.error ? { diskError: diskBank.error } : {}),\n            ...(bankError ? { error: bankError } : {}),\n        },\n        memoryBridge: bridgeStats,\n        neuralPatterns: neuralStats,\n        consistency: {\n            sonaTracksGlobal: null,\n            sonaTracksGlobalDelta: null,\n            comparable: false,\n            comparison: 'not-comparable: bounded process buffer versus lifetime events',\n            notes,\n        },\n        reportingContract: 'store-scoped-learning-stats-v1',\n        generatedAt: new Date().toISOString(),\n    };\n}";
export const NEURAL_OLD = "export function getNeuralStoreStats() {\n    const store = loadNeuralStore();\n    const patterns = Object.values(store.patterns ?? {});\n    const byType = {};\n    for (const p of patterns) {\n        const t = p.type || 'unknown';\n        byType[t] = (byType[t] ?? 0) + 1;\n    }\n    return {\n        patternCount: patterns.length,\n        byType,\n        modelCount: Object.values(store.models ?? {}).length,\n        source: '.claude-flow/neural/patterns.json (loadNeuralStore)',\n    };\n}";
export const NEURAL_NEW = "export function getNeuralStoreStats() {\n    // ruflo-source-patch (ruvnet/ruflo#2245): report the actual file, not ReasoningBank's patterns.json.\n    const source = getNeuralPath() + ' (loadNeuralStore)';\n    try {\n        const present = existsSync(getNeuralPath());\n        const store = present ? JSON.parse(readFileSync(getNeuralPath(), 'utf-8')) : { models: {}, patterns: {} };\n        if (!store || typeof store !== 'object' || Array.isArray(store)\n            || !store.models || typeof store.models !== 'object' || Array.isArray(store.models)\n            || !store.patterns || typeof store.patterns !== 'object' || Array.isArray(store.patterns)) {\n            throw new Error('Invalid neural models/patterns store shape');\n        }\n        const patterns = Object.values(store.patterns);\n        const byType = {};\n        for (const p of patterns) {\n            if (!p || typeof p !== 'object') throw new Error('Invalid neural pattern');\n            const t = p.type || 'unknown';\n            byType[t] = (byType[t] ?? 0) + 1;\n        }\n        return { patternCount: patterns.length, byType, modelCount: Object.keys(store.models).length,\n            source, scope: 'neural model artefacts; independent of LocalReasoningBank',\n            available: true, readStatus: present ? 'ok' : 'absent' };\n    } catch (error) {\n        return { patternCount: null, byType: {}, modelCount: null, source,\n            available: false, readStatus: 'error', error: String(error?.message || error) };\n    }\n}";
export const DESCRIPTION_OLD = "    description: 'One honest view across the four learning stat sources: globalStats (`.claude-flow/neural/stats.json`), the in-memory SONA coordinator, memory-bridge AgentDB entries, and the neural-patterns store. Each sub-view names its source path. The `consistency` block notes cross-store drift (e.g. globalStats reports N patterns but neural_patterns is empty). See ADR-075. Use when calling the four narrow aggregators (`hooks_intelligence stats`, `memory_stats`, `neural_status`, the SONA coordinator getter) one at a time is wrong because they each see only their own slice and cross-store drift goes silent \u2014 this tool surfaces that drift in the `consistency` block, which the narrow APIs cannot.',";
export const DESCRIPTION_NEW = "    description: 'Read store-scoped learning statistics: persisted event counters, process snapshots, bounded SONA activity, retained ReasoningBank patterns, AgentDB entries and the separate neural models.json store. Unknown counts are null; different scopes are not compared as equal. Reports retention and activity, not measured model improvement. ruflo-source-patch (ruvnet/ruflo#2245).',";
export const LEARNING_STATS_ENTRIES = [
  { id: 'ruflo-learning-stats/tool-contract', target: 'ruflo-learning-stats',
    suffix: ['@claude-flow', 'cli', 'dist', 'src', 'mcp-tools', 'hooks-tools.js'],
    edits: [{ find: DESCRIPTION_OLD, replace: DESCRIPTION_NEW }] },
  { id: 'ruflo-learning-stats/unified', target: 'ruflo-learning-stats',
    suffix: ['@claude-flow', 'cli', 'dist', 'src', 'memory', 'intelligence.js'],
    edits: [{ find: UNIFIED_OLD, replace: UNIFIED_NEW }] },
  { id: 'ruflo-learning-stats/neural-source', target: 'ruflo-learning-stats',
    suffix: ['@claude-flow', 'cli', 'dist', 'src', 'mcp-tools', 'neural-tools.js'],
    edits: [
      { find: NEURAL_OLD, replace: NEURAL_NEW, optional: true },
      { find: NEURAL_OLD.replace('neural/patterns.json', 'neural/models.json'), replace: NEURAL_NEW, optional: true },
    ] },
  { id: 'ruflo-learning-stats/pattern-namespace', target: 'ruflo-learning-stats',
    suffix: ['@claude-flow', 'cli', 'dist', 'src', 'memory', 'memory-bridge.js'],
    edits: [{ find: "        'default', 'patterns', 'claude-memories', 'auto-memory',",
      replace: "        'default', 'pattern', 'patterns', 'claude-memories', 'auto-memory', // ruflo-source-patch (ruvnet/ruflo#2245)" }] },
];
